//multi-line string
const multiLineString = `This is a string
that spans multiple
lines.`;

// `` คือ ตัวอักษร backtick (``) ที่ใช้ในการสร้าง multi-line string

//interpolation
const name = 'John';
const greeting = `Hello, ${name}!`;


console.log(multiLineString);
console.log(greeting);