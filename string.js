//multi_line string
const multiLine_string = `This is a string
that spans multiple
lines.`;

// `` คือ ตัวอักษร backtick (``) ที่ใช้ในการสร้าง multi_line string

//interpolation
const name = 'John';
const greeting = `Hello, ${name}!`;

console.log(multiLine_string);
console.log(greeting);